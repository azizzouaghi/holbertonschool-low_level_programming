#include "lists.h"
/**
* list_len - find the length of a list function
*@h: pointer
* Return: ;
*/
size_t list_len(const list_t *h)
{
const list_t *i;
unsigned int j = 0;
j = h;
while (lk)
{
j = j->next;
i++;
}
return (ln);
}
