#include <stdio.h>
/**
* main - base 16
* description: base 16
* Return: always 0
*/
int main(void)
{
int s, n;
for (s = 0; s <= 9; s++)
putchar(s);
for (n = 'a'; n <= 'f'; s++)
putchar(n);
putchar('\n');
return (0);
}
